# Пользовательские XDG defaults, которые относятся к desktop-интеграции приложений.
{
  xdg.mimeApps = {
    enable = true;

    defaultApplications = {
      "inode/directory" = [ "org.gnome.Nautilus.desktop" ];
    };
  };
}
